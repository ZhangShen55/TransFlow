## 1. 项目基础

- [x] 1.1 创建 `src/transflow` 包、FastAPI 应用工厂、设计中定义的模块边界和测试目录；验证包可以导入，空应用能在 `transflow` Conda 环境中启动。
- [x] 1.2 在 `pyproject.toml` 中添加固定版本的 Python 3.12 运行和开发依赖以及可复现安装元数据；验证全新的 `conda run -n transflow` 安装和依赖检查成功。
- [x] 1.3 添加基础格式化、代码检查、类型检查和 pytest 配置；验证配置的质量检查命令可在项目骨架上成功运行。
- [x] 1.4 添加 `.gitignore`、`.dockerignore` 和受版本控制的模型放置说明，确保 `model/Hz-MT2` 权重不进入 Git 和 Docker 构建上下文；验证示例模型文件保持忽略状态。

## 2. 配置与 API 领域

- [x] 2.1 为 `server`、`api`、`scheduler`、`inference`、`generation`、`response`、`logging` 和 `metrics` 实现类型化启动配置及 `TRANSFLOW_` 环境变量覆盖；验证测试覆盖解析、覆盖、缺失值、范围错误和跨字段安全校验。
- [x] 2.2 创建项目级 `config.toml`，提供保守初始值和说明用途、单位及调优影响的简短相邻注释；验证配置可无警告地加载为类型化配置。
- [x] 2.3 实现完整的 Hz-MT2 语言代码注册表和语言全名提示词映射；验证单元测试接受全部指定代码，将 `ar` 映射为阿拉伯语，并拒绝 `ra`、未知代码和重复目标语言。
- [x] 2.4 实现请求、成功响应、结果内容和公开错误模式，包括可配置模型标签和 ID 前缀；验证模式测试覆盖 1 条与 120 条文本、配置上限溢出、目标语言限制和响应序列化。

## 3. 推理与调度

- [x] 3.1 定义异步推理后端协议和确定性模拟后端，包括健康检查、生成和中止操作；验证测试无需 GPU 即可模拟延迟、乱序完成、失败和取消。
- [x] 3.2 实现兼容 OpenAI API 的 vLLM 客户端，包括连接复用、明确的聊天提示词与生成参数、后端请求 ID、健康检查、期限、安全重试分类和中止支持；验证模拟 HTTP 测试覆盖成功、未就绪、异常响应、接收前重试、超时和取消。
- [x] 3.3 实现有界请求准入、可配置文本位置切分、已准入请求间的公平推进、全局在途限制和关闭行为；验证调度器测试覆盖默认 64 请求限制、队列满拒绝、120/60 两分片、公平性、期限传递和断开连接清理。
- [x] 3.4 使用按语言与文本索引定位的结果矩阵实现翻译编排，包括精确空字符串本地完成、混合源语言提示词和原子失败处理；验证测试在重复字符串、首尾空值、同语言目标、乱序完成及单单元失败场景下保持顺序和数量。

## 4. FastAPI 与可观测性

- [x] 4.1 实现 `POST /translate`，将校验、准入、后端就绪、超时和内部失败映射为指定 HTTP 状态；验证端点契约测试覆盖 200、422、429、503 和 504，且不会返回部分成功响应。
- [x] 4.2 实现应用生命周期及 `/health/live`、`/health/ready`，根据有效配置和 vLLM 健康状态控制就绪；验证生命周期测试覆盖后端冷启动、恢复和平稳关闭。
- [x] 4.3 添加带请求关联的结构化日志和 `/metrics` 指标，覆盖流量、请求维度、队列状态、推理活动、耗时、取消和失败；验证测试检查必需字段，并确认默认不包含原文、提示词和译文。
- [x] 4.4 添加 API 使用及运维文档，使用约定的 `ar` 请求与响应示例；验证文档示例符合已实现的模式。

## 5. 容器部署

- [x] 5.1 为 FastAPI 服务创建 `docker/Dockerfile.api`，使用非 root 用户和健康检查，且不包含 CUDA 或模型层；验证镜像构建成功并报告预期的软件包版本和用户。
- [x] 5.2 创建 `docker/compose.yaml`，使用 API 镜像和 `vllm/vllm-openai:v0.28.0`、只读 `/model` 挂载、张量并行度 1、明确的生成与容量参数、私有 vLLM 网络、API 端口发布、重启策略和至少 180 秒模型冷启动宽限；验证 Compose 配置检查通过。
- [x] 5.3 在可用 GPU 上启动默认 Compose 服务，验证 vLLM 健康状态、API 就绪、模型端口私有性、模型加载和真实 `POST /translate` 响应，然后平稳停止服务。
- [x] 5.4 添加并记录可选的多 GPU Compose 覆盖和推理端点路由配置，默认不启用；验证合并后的 Compose 配置将不同模型副本分配到指定 GPU。

## 6. 正确性与容量验证

- [x] 6.1 为全部 `translation-api` 和不依赖硬件的 `translation-runtime` 场景补齐单元测试与模拟后端集成测试；验证完整 pytest、代码检查、格式检查和类型检查通过。
- [x] 6.2 添加真实模型冒烟测试，覆盖就绪、英语与阿拉伯语生成、混合源语言、精确空字符串、目标语言顺序和 120 条结果数量；验证其在固定 vLLM 镜像上通过。
- [x] 6.3 为分片大小 `8/16/32/60`、并发量 `1/8/16/32/64`、真实字幕分布和最大请求结构实现可重复负载测试工具；验证样例运行可输出机器可读报告，包含吞吐量、p50/p95/p99 延迟、排队时间、token 数、峰值显存、错误、超时、取消、结果数量错误和 OOM 状态。
- [x] 6.4 在无其他负载的指定 GPU 上运行渐进负载矩阵，保留报告、淘汰失败候选，并根据最佳成功配置更新生产默认值；验证 64 并发验收配置在记录的期限内完成，且无崩溃、OOM 或输出结构错误。
- [x] 6.5 运行严格 OpenSpec 校验，以及文档中的纯净环境安装、测试、镜像构建、Compose 冒烟和 API 示例命令；验证全部命令通过，仓库不包含模型权重、生成的密钥或临时压测产物。
